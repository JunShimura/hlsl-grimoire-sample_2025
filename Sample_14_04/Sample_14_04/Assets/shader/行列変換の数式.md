# 行列変換の数式

## 4x4 変換行列の構造

HLSLの`float4x4`行列は以下のように2次元配列として並んでいます：

```
float4x4 mWorld の構造：

	   [列0] [列1] [列2] [列3]
[行0]  m00   m01   m02   m03
[行1]  m10   m11   m12   m13
[行2]  m20   m21   m22   m23
[行3]  m30   m31   m32   m33

インデックスでのアクセス：
mWorld[0] = [m00, m01, m02, m03]  // 1行目（X軸方向の基底ベクトル + X平行移動）
mWorld[1] = [m10, m11, m12, m13]  // 2行目（Y軸方向の基底ベクトル + Y平行移動）
mWorld[2] = [m20, m21, m22, m23]  // 3行目（Z軸方向の基底ベクトル + Z平行移動）
mWorld[3] = [m30, m31, m32, m33]  // 4行目（平行移動成分）
```

## 各成分の役割

```
[  回転・スケール（X軸）  回転・スケール（Y軸）  回転・スケール（Z軸）  平行移動  ]

[ m00  m01  m02  m03 ]  ← X成分の変換係数
[ m10  m11  m12  m13 ]  ← Y成分の変換係数
[ m20  m21  m22  m23 ]  ← Z成分の変換係数
[ m30  m31  m32  m33 ]  ← 平行移動（Translation）+ W成分
   ↑    ↑    ↑    ↑
   X    Y    Z    W
```

### 平行移動成分の取得

```hlsl
float4 objectPos = mWorld[3];
// objectPos.x = mWorld[3][0] = m30 (X座標)
// objectPos.y = mWorld[3][1] = m31 (Y座標)
// objectPos.z = mWorld[3][2] = m32 (Z座標)
// objectPos.w = mWorld[3][3] = m33 (通常は1)
```

## 行列とベクトルの乗算

### 元のベクトル

変換前のベクトルを以下のように定義します：

```
v = (x, y, z, w)
```

### mul(matrix, vector) の場合

HLSLで`mul(mWorld, vsIn.pos)`のように記述した場合、以下の計算が行われます：

```
v' = mWorld × v
```

## 変換後のベクトルの各成分の計算式

### 変換後の x' の計算式

```
x' = m00 * x + m01 * y + m02 * z + m03 * w
```

### 変換後の y' の計算式

```
y' = m10 * x + m11 * y + m12 * z + m13 * w
```

### 変換後の z' の計算式

```
z' = m20 * x + m21 * y + m22 * z + m23 * w
```

### 変換後の w' の計算式

```
w' = m30 * x + m31 * y + m32 * z + m33 * w
```

## 行列形式での表現

```
[x']   [m00  m01  m02  m03]   [x]
[y'] = [m10  m11  m12  m13] × [y]
[z']   [m20  m21  m22  m23]   [z]
[w']   [m30  m31  m32  m33]   [w]
```

## 実際の変換例

### ワールド変換（sample.fxのコード）

```hlsl
psIn.pos = mul(mWorld, vsIn.pos);
```

この変換では：
- vsIn.pos = (x, y, z, 1) ← 頂点座標（wは通常1）
- m00～m22: 回転やスケールの情報
- m03, m13, m23: 平行移動成分（実際には通常0）
- m30, m31, m32: 平行移動成分（オブジェクトの位置）
- m33: 通常は1

具体的には：
```
x' = m00*x + m01*y + m02*z + m03*1
y' = m10*x + m11*y + m12*z + m13*1
z' = m20*x + m21*y + m22*z + m23*1
w' = m30*x + m31*y + m32*z + m33*1
```

### オブジェクト位置の取得（sample.fxのコード）

```hlsl
float4 objectPos = mWorld[3];
```

これは行列の4行目（平行移動成分）を直接取得しています：
```
objectPos = (m30, m31, m32, m33)
```

この値がオブジェクトのワールド座標系での位置を表します。

## 変換の連鎖

sample.fxでは以下のように複数の行列を連続して適用しています：

```hlsl
psIn.pos = mul(mWorld, vsIn.pos);  // ローカル座標 → ワールド座標
psIn.pos = mul(mView, psIn.pos);   // ワールド座標 → カメラ座標
psIn.pos = mul(mProj, psIn.pos);   // カメラ座標 → スクリーン座標
```

各変換で上記の数式が適用され、最終的にスクリーン座標に変換されます。
